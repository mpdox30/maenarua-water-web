# 2026-07-14 เพิ่ม — ก่อนหน้านี้ repo นี้ไม่มี .gitignore เลย (ยืนยันจาก git status ที่ track ไฟล์
# runtime-generated จำนวนมาก) เพิ่มไฟล์นี้ตอนต่อ SAR crop classification pipeline เข้ากับ git
# เพราะมีไฟล์ runtime state/credential ที่ห้าม commit เกิดขึ้นใหม่จากงานนี้โดยเฉพาะ

# --- SAR crop classification: runtime artifacts (regenerable, ไม่ควร commit) ---
01_data/gis/sar_rasters/
01_data/gis/sar_output/
01_data/gis/.sar_last_classified
01_data/gis/.sar_last_classified_test

# --- GEE Service Account credentials (ห้าม commit เด็ดขาด — ดู gee_auth.py) ---
# แนะนำให้เก็บ key file ไว้นอกโฟลเดอร์ repo นี้ตั้งแต่แรกอยู่แล้ว (ดู docstring gee_auth.py) แต่กัน
# ไว้อีกชั้นเผื่อมีใครวางไว้ในนี้โดยไม่ตั้งใจ
*service-account*.json
*-key.json
gee-credentials*.json

# --- Python ---
__pycache__/
*.pyc
.venv/

# --- Logs (regenerable) ---
01_data/scripts and code/pipeline/logs/*.txt

# --- 2026-07-19 เพิ่ม — โมเดลไบนารีขนาดใหญ่เกินลิมิต GitHub (100MB/ไฟล์) ---
# ถูกลบออกจาก git history ทั้งหมดแล้วด้วย git filter-repo (2026-07-19) เพราะ push ครั้งแรกโดน
# GitHub ปฏิเสธ (pre-receive hook declined — GH001 file too large) ไฟล์ยังอยู่บนดิสก์ตามปกติ
# (data_pipeline.py ยังโหลดใช้ได้ปกติ) แค่ไม่ track ใน git อีกต่อไป ถ้าต้องสำรอง/แชร์โมเดลพวกนี้ให้ใช้
# Google Drive หรือ Git LFS แยกต่างหาก ไม่ใช่ commit ตรงเข้า repo นี้
01_data/scripts and code/Water_demand/active/catboost_models.pkl
01_data/scripts and code/Water_demand/active/rf_model_v3b_final.pkl

# --- 2026-07-20 เพิ่ม — ไฟล์นี้มี password จริงของ API สถานีโทรมาตร (กช.สสน.) ฝังอยู่ตรงๆ ---
# หลุดติดไปกับ git commit ก่อนหน้านี้ จนถูกดันขึ้น GitHub และ repo ถูกเปลี่ยนเป็น public ไปแล้วตอนที่
# พบ (2026-07-20) ได้ลบออกจาก git history ทั้งหมดด้วย git filter-repo แล้ว + ขอรหัสผ่านใหม่จาก
# กช.สสน. มาแทนตัวเดิมที่ถือว่ารั่วแล้ว ไฟล์ยังอยู่บนดิสก์ตามปกติ (ไว้เป็น reference คนละจุดกับ
# environment variable TELEMETRY_API_URL ที่ telemetry_feature.py อ่านจริง) แค่ไม่ track ใน git อีกต่อไป
01_data/telemetry/Tele_API_Address.txt

# --- 2026-07-25 เพิ่ม — โฟลเดอร์ทำงานชั่วคราวสำหรับปรับปรุงโมเดล Reservoir Inflow (NSE) ---
# สำเนาแยกของ training CSV/notebook/metadata ทำงาน/ทดลองแยกจากของจริงทั้งหมด ไม่แตะไฟล์ต้นฉบับใน
# 01_data/scripts and code/Reservoir_inflow/active/ เลย ลบทิ้งได้เมื่อตัดสินใจ deploy ผลลัพธ์จริงแล้ว
_inflow_model_improve_wip/

# --- 2026-07-25 เพิ่ม — โฟลเดอร์ทำงานชั่วคราวสำหรับ refactor สถาปัตยกรรมเว็บ (Web Components) ---
# สำเนาแยกของ 03_website/ ทำงาน/ทดสอบแยกจากของจริงทั้งหมด ไม่ให้กระทบเว็บ live หรือชน
# กับ auto-push ของ monitoring_data_builder.py (รันทุก 10-15 นาที, git add เฉพาะไฟล์ที่รู้จักเท่านั้น
# แต่กันไว้อีกชั้นไม่ให้ขึ้น git status เป็น noise) ลบทิ้งได้เมื่องาน merge กลับเข้า 03_website/ จริงแล้ว
_website_refactor_wip/

# --- 2026-07-20 เพิ่ม — เอกสารประกอบ manuscript (draft) ---
# ลบออกจาก git history ทั้งหมดแล้วด้วย git filter-repo (2026-07-20) ตามที่ผู้ใช้ขอ เพราะ repo
# เปลี่ยนเป็น public แล้ว และ PROJECT_MEMORY.md ระบุชัดว่าห้ามเอา draft manuscript ไปแสดงในส่วน
# สาธารณะ — research.html อ้างอิงไฟล์ในนี้ด้วย relative path "../04_docs_research/..." แต่ path นี้
# พังอยู่แล้วบน GitHub Pages ที่ deploy จริง (workflow publish แค่ 03_website/ เป็น root ไม่รวม
# โฟลเดอร์นี้อยู่แล้ว) ลบออกจึงไม่กระทบเว็บ live เพิ่มเติม ไฟล์ยังอยู่บนดิสก์ตามปกติสำหรับดูผ่าน local
# server เท่านั้น แค่ไม่ track ใน git อีกต่อไป
04_docs_research/

# --- 2026-08-12 เพิ่ม — ไฟล์ backup อัตโนมัติของไฟล์ทางการอ่างเก็บน้ำ (สร้างทุกครั้งก่อนเขียนทับจริง
# โดย reservoir_official_file_writer.py/create_new_month_official_file.py) ไม่ต้อง track ใน git --
# มีเยอะมาก (เกือบทุกวัน) และของจริงที่แก้แล้วอยู่ใน .xlsx หลักซึ่ง track อยู่แล้ว ต้อง ignore ไว้ก่อน
# เพิ่มขั้นตอน git add แบบกว้าง (ทั้งโฟลเดอร์ inflow/) ให้ run_reservoir_daily_orchestration.bat ได้
# อย่างปลอดภัย ไม่ดึง backup พวกนี้ติดไปด้วย
01_data/Reservoirs/inflow/*/*.bak_before_*
